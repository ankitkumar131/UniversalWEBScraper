# TEMPLATE — replace values before applying (base64 or use `kubectl create secret`):
#   kubectl create secret generic uws-secrets \
#     --from-literal=twocaptcha-api-key=... \
#     --from-literal=openai-api-key=...
apiVersion: v1
kind: Secret
metadata:
  name: uws-secrets
type: Opaque
data:
  twocaptcha-api-key: ""
  openai-api-key: ""
